# A line fitted over each window, plus the value it ends on. Which columns get
# which comes from the schema: the fit on the numeric features, the last value
# on all of them.
_target_: dialysisml.pipeline.transformers.RegressionFeatures
