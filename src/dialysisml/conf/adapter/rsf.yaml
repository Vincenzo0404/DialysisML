# The adapter takes its hyperparameters directly: a sweep overrides
# `model.hidden_size`, not `model.model_config.hidden_size`.
_target_: dialysisml.adapters.RSFAdapter


n_estimators: 200
n_jobs: 5
min_samples_split: 10
min_samples_leaf: 15
seed: 42
